# Server Configuration
PORT=3000
HOST=0.0.0.0

# Database Configuration
# Option 1: Use full DATABASE_URL (recommended)
# DATABASE_URL=mysql://sae_user:sae_password@localhost:3306/sae_project_db

# Option 2: Use individual database variables (if DATABASE_URL is blank)
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=sae_user
DB_PASSWORD=sae_password
DB_DATABASE=sae_project_db
