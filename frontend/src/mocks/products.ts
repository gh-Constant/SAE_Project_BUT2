export interface ProductMock {
  id: number;
  name: string;
  description: string;
  price: number;
  stock: number;
  imageUrl: string;
  locationId: number;
}

export const PRODUCTS: ProductMock[] = [
];